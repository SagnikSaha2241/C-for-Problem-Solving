#include<stdio.h>
int main()
{
    float r;
    float area,peremeter;
    printf("Enter The radius: ");
    scanf("%f",&r);
    area=3.14*r*r;
    peremeter=2*3.14*r;
    printf("The area and the peremeter is: %f %f",area,peremeter);
    return 0;
}
